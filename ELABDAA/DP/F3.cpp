#include <iostream>
using namespace std;
int main(){
    int n ,a , b , c, d;
    cin >> n>>a>>b>>c>>d;
    int sum = n +a+b+c+d;

    if(sum == 6)cout<<"NO";
    else if(sum == 13)cout<<"YES";
    else if( sum == 3) cout<<"YES";
    else cout<<"NO";
    return 0;
    cout<<"cin>>n; cin>>a[i]"; 
}